import 'package:flutter/material.dart';

class MatchStatePage extends StatelessWidget {
  const MatchStatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('MatchStatePage'),
      ),
    );
  }
}
